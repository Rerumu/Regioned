[package]
name = "regioned"
version = "0.1.0"
edition = "2021"

[features]
dot = []

[dependencies]
arena = { git = "https://github.com/Rerumu/Arena" }
hashbrown = "=0.13.2"
tinyvec = { version = "=1.6.0", features = ["alloc"] }

[dev-dependencies]
criterion = "=0.4.0"

[[bench]]
name = "basic"
harness = false
