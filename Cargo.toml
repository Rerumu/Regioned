[package]
name = "regioned"
version = "1.0.0"
edition = "2021"

[features]
dot = []

[dependencies]
tinyvec = { version = "=1.6.0", features = ["alloc"] }

[dependencies.arena]
git = "https://github.com/Rerumu/Arena"
rev = "91f3d06"

[dev-dependencies]
criterion = "0.5.1"

[[bench]]
name = "basic"
harness = false
